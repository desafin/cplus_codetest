#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(0);
	int casenum;
	cin>>casenum;
	deque<pair<int,int>> d;
	for(int i=0;i<casenum;i++)
	{
		int num;
		cin>>num;
		d.push_back({i,num});
	}

	while(!d.empty())
	return 0;
}